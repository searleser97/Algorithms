#include<bits/stdc++.h>

using namespace std;

int n; // max node id >= 0
typedef int Weight;
typedef pair<int, int> NeighCost;
typedef pair<int, NeighCost> ady;

vector<int> parent;
vector<int> dist;

void Dijkstra(int src) {
    
}

int main() {
    cin >> n;
    ady.resize(n);
    parent.resize(n);
    dist.resize(n);
    return 0;
}